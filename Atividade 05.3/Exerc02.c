#include <stdio.h>

main(){
    /*
    2. Escreva um programa que leia um número inteiro e depois imprima a mensagem “Valor
    lido: ”, seguido do valor inteiro lido. Use apenas um comando printf().
    */
    int valor=0;
    // Cria a variável valor do tipo int
    scanf("%d", &valor);
    //Ler um valor digitado
    printf("Valor lido: %d", valor);
    // Mostra o valor na tela
}